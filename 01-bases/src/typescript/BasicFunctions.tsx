const add = (a: number, b: number): number => {
    return a + b
}

export default function BasicFunctions() {
    return (
        <>
            <h3>Basic Functions</h3>
            <span>add(2, 3) = {add(2, 3)}</span>
        </>
    )
}
